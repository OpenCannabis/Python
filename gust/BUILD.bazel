package(
    default_visibility = ["//visibility:public"],
)

load(
    "//config:rules.bzl",
    "ocp_module",
)

load(
    "@rules_pkg//:pkg.bzl",
    "pkg_tar",
)


pkg_tar(
    name = "gust-init",
    srcs = ["__init__.py"],
    package_dir = "gust/",
)
