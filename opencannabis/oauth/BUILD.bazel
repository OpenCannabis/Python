
package(
    default_visibility = ["//visibility:public"],
)

load(
    "//config:rules.bzl",
    "ocp_py",
)

package = "@ocp//opencannabis/oauth"


## Protocol Definitions
[
    ocp_py(name = n, target = "%s:%s" % (package, n)) for n in [
        "AuthorizationScope",
        "Client",
    ]
]
